import { style } from "treat";
import { defaultSpacing } from "../../style/constants";

export const address = style({
  margin: `${defaultSpacing}px 0`,
});

export const map = style({
  maxWidth: "100%",
});
